# Use the head() function to display the first 5 rows of the data
# We can use head(10), head(12) etc...
data.head()

# Use the tail() function to display the last 5 rows of the data
# We can use tail(10), tail(12) etc...
data.tail()

# Print the shape of the data and classes
print(data.shape)

print(data['Class'].unique())
